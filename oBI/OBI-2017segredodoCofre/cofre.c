#include <stdio.h>
int main(){
    int N = 0, M = 0;
    scanf("%d %d",&N ,&M);
    int vet[N];
    for(int i = 0;i < N;i++){
        scanf("%d", vet[i]);
    }
    int atual = 0;
    for(int i = 0;i < M; i++){
        autual = vet[i];
        if
    }
}