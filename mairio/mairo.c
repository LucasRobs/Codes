#include <stdio.h>
int main(){
    int size = 0;
    scanf("%d", &size);
    vet[size];
    for(int i = 0; i < size; i++){
        scanf("%d", vet[i]);
    }
    for(int i = 0;i < size;i++){
        if(vet[i + 1] - vet[i] == 1)
    }
}